# Project knowledge (`docs/`)

BMad BMM config sets `project_knowledge` to this directory. Use it for **brownfield notes**, runbooks, or ADRs that are not part of the PRD/UX/architecture funnel.

**Current app docs:** start at the repo [README](../README.md). **Planning and delivery specs** live under:

- `_bmad-output/planning-artifacts/` — PRD, UX, architecture, epics, readiness
- `_bmad-output/implementation-artifacts/` — sprint tracking

No additional project-specific files have been added here yet.
